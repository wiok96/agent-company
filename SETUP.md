# 🚀 دليل الإعداد السريع لـ AACS V0

## خطوات الإعداد (5 دقائق)

### 1. إعداد GitHub Secrets ⚙️

اذهب إلى إعدادات المستودع:
```
Settings > Secrets and variables > Actions > New repository secret
```

أضف هذه الأسرار:

| اسم السر | القيمة | مطلوب |
|---------|--------|--------|
| `AI_PROVIDER` | `groq` | ✅ |
| `AI_API_KEY` | `api_key` | ✅ |
| `GITHUB_TOKEN` | يتم إنشاؤه تلقائياً | ✅ |
| `TELEGRAM_BOT_TOKEN` | رمز بوت Telegram | ❌ |
| `TELEGRAM_CHAT_ID` | معرف المحادثة | ❌ |

### 1.1 تفعيل صلاحيات GitHub Actions 🔐

اذهب إلى:
```
Settings > Actions > General > Workflow permissions
```

اختر: **Read and write permissions** ✅

### 2. تفعيل GitHub Pages 📄

```
Settings > Pages > Source: GitHub Actions
```

### 3. تشغيل أول اجتماع 🎯

```
Actions > AACS Manual Meeting > Run workflow
```

أدخل:
- **أجندة الاجتماع**: `اجتماع تجريبي أول`
- **وضع التصحيح**: `true`
- **الأولوية**: `normal`

### 4. مراقبة النتائج 📊

بعد 2-3 دقائق:
- تحقق من مجلد `meetings/` للمحاضر
- تحقق من `board/tasks.json` للمهام الجديدة
- زر لوحة التحكم على GitHub Pages

## الاختبار المحلي (اختياري)

```bash
# 1. انسخ قالب التكوين
cp .env.example .env

# 2. أضف مفاتيح API الحقيقية في .env
# 3. شغل الاجتماع
python scripts/run_meeting.py
```

## التحقق من النجاح ✅

### علامات النجاح:
- ✅ GitHub Action ينتهي بنجاح
- ✅ ملفات جديدة في `meetings/meeting_YYYYMMDD_HHMMSS/`
- ✅ مهام جديدة في `board/tasks.json`
- ✅ لوحة التحكم تعرض البيانات

### في حالة الفشل:
1. **تحقق من GitHub Secrets** - تأكد من وجود AI_API_KEY
2. **راجع سجلات GitHub Actions** - تحقق من الأخطاء
3. **تأكد من صحة مفتاح Groq API** - يجب أن يبدأ بـ gsk_
4. **تفعيل صلاحيات الكتابة** - Settings > Actions > General > Read and write permissions

## الاجتماعات التلقائية 🕐

بعد الإعداد، سيعقد النظام اجتماعات تلقائية:
- **كل 6 ساعات**: 00:00, 06:00, 12:00, 18:00 UTC
- **تلقائياً**: بدون تدخل بشري
- **مع حفظ**: جميع المخرجات في Git

## الدعم والمساعدة 🆘

إذا واجهت مشاكل:
1. تحقق من ملف `IMPLEMENTATION_STATUS.md`
2. راجع سجلات GitHub Actions
3. تأكد من إعدادات GitHub Pages

---

**🎉 مبروك! نظام AACS V0 جاهز للعمل**